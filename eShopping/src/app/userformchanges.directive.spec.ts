import { UserformchangesDirective } from './userformchanges.directive';

describe('UserformchangesDirective', () => {
  it('should create an instance', () => {
    const directive = new UserformchangesDirective();
    expect(directive).toBeTruthy();
  });
});
